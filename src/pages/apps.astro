---
import Base from "../layouts/Base.astro";
import ToolMap from "../components/ToolMap";
import { getCollection } from "astro:content";

const allTools = await getCollection("tools");

const tools = allTools.map((t) => ({
  id: t.id,
  name: t.data.name,
  maker: t.data.maker,
  category: t.data.category,
  status: t.data.status,
  claim: t.data.claim,
  metrics: t.data.metrics,
  updated: t.data.updated,
  reviewed: t.data.reviewed,
  source: t.data.source,
}));
---

<Base title="Apps — GrabStack" description="The full, searchable list of every major AI application.">
  <section class="py-6 sm:py-10">
    <h1 class="font-sans text-2xl font-bold tracking-tight sm:text-4xl">
      Apps<span class="text-accent">.</span>
    </h1>
    <p class="mt-2 max-w-xl text-base text-muted">
      The full, searchable list of every major AI application.
    </p>
  </section>

  <ToolMap client:load tools={tools} />
</Base>
